model: GRU4Rec
dataset: mssd

data_path: "../data/"

USER_ID_FIELD: session_id
ITEM_ID_FIELD: item_id
TIME_FIELD: timestamp

load_col:
    inter: [session_id, item_id, timestamp]

embedding_size: 128
hidden_size: 128

dropout_prob: 0.2
learning_rate: 0.001
num_layers: 2
pooling_mode: sum

